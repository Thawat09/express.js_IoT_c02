import { cellType, DataTableOptions, headerCellType, nodeType, renderOptions } from "./interfaces";
export declare const headingsToVirtualHeaderRowDOM: (headings: any, columnSettings: any, columnWidths: any, { classes, hiddenHeader, sortable, scrollY }: DataTableOptions, { noColumnWidths, unhideHeader }: renderOptions) => {
    nodeName: string;
    childNodes: any;
};
export declare const dataToVirtualDOM: (id: string, headings: headerCellType[], rows: {
    row: cellType[];
    index: number;
}[], columnSettings: any, columnWidths: number[], rowCursor: (number | false), { classes, hiddenHeader, header, footer, sortable, scrollY, rowRender, tabIndex }: DataTableOptions, { noColumnWidths, unhideHeader, renderHeader }: renderOptions) => nodeType;
