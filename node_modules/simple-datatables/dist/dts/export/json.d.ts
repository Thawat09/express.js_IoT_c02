import { DataTable } from "../datatable";
/**
 * Export table to JSON
 */
interface jsonUserOptions {
    download?: boolean;
    skipColumn?: number[];
    replacer?: null | ((arg: any) => string) | (string | number)[];
    space?: number;
    selection?: number | number[];
    filename?: string;
}
export declare const exportJSON: (dataTable: DataTable, userOptions?: jsonUserOptions) => string | false;
export {};
